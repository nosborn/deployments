---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: webhook
  namespace: flux-system
spec:
  dnsNames:
    - flux.osborn.io
  duration: 168h
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  privateKey:
    algorithm: ECDSA
    size: 256
  renewBefore: 8h
  secretName: webhook-tls
