---
apiVersion: batch/v1
kind: CronJob
metadata:
  labels:
    app.kubernetes.io/component: forgejo-checkpoint
    app.kubernetes.io/instance: forgejo
    app.kubernetes.io/name: forgejo
  name: forgejo-checkpoint
  namespace: forgejo
spec:
  concurrencyPolicy: Forbid
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app.kubernetes.io/component: forgejo-checkpoint
            app.kubernetes.io/instance: forgejo
            app.kubernetes.io/name: forgejo
        spec:
          containers:
            - args:
                - /data/db.sqlite3
                - PRAGMA wal_checkpoint(TRUNCATE);
              image: docker.io/alpine/sqlite:3.51.2
              imagePullPolicy: IfNotPresent
              name: checkpoint
              resources: {}
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop:
                    - ALL
                readOnlyRootFilesystem: true
                runAsGroup: 1000
                runAsNonRoot: true
                runAsUser: 1000
              volumeMounts:
                - mountPath: /data
                  name: data
          imagePullSecrets:
            - name: docker-hub
          restartPolicy: Never
          securityContext:
            fsGroup: 1000
            seccompProfile:
              type: RuntimeDefault
          volumes:
            - name: data
              persistentVolumeClaim:
                claimName: forgejo-data
      ttlSecondsAfterFinished: 21600
  schedule: 0 */6 * * *
  successfulJobsHistoryLimit: 3
  timeZone: Etc/UTC
