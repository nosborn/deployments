---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: runner-data
  namespace: gitea-actions
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
