---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: searxng
  namespace: searxng
spec:
  filterTags:
    extract: $date
    pattern: ^(?P<date>\d{4}\.\d+\.\d+)-[a-f0-9]+$
  imageRepositoryRef:
    name: searxng
  policy:
    semver:
      range: ">=2026.0.0"
