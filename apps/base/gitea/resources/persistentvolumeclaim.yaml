---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitea-valkey
  namespace: gitea
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
  volumeMode: Filesystem
