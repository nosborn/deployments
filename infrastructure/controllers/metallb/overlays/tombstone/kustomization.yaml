---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - patch: |-
      - op: replace
        path: /spec/addresses
        value:
          - ***REMOVED***/32
    target:
      kind: IPAddressPool
