---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: openclaw
  namespace: openclaw
spec:
  filterTags:
    pattern: ^\d{4}\.\d+\.\d+$
  imageRepositoryRef:
    name: openclaw
  policy:
    semver:
      range: ">=2026.0.0"
