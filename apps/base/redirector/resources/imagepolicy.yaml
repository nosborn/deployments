---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: nginx
  namespace: redirector
spec:
  filterTags:
    extract: $version
    pattern: ^(?P<version>\d+\.\d+\.\d+)-alpine-otel$
  imageRepositoryRef:
    name: nginx
  policy:
    semver:
      range: ">=1.31.0 <2.0.0"
