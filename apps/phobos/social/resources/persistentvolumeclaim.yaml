---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pds-data
  namespace: social
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 10Gi
  storageClassName: ""
  volumeMode: Filesystem
  volumeName: pds-data
