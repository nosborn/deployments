---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: miasma
  namespace: miasma
spec:
  imageRepositoryRef:
    name: miasma
  policy:
    semver:
      range: ">=0.2.0 <1.0.0"
