---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tt-rss-data
  namespace: rss
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-storage
  volumeMode: Filesystem
  volumeName: tt-rss-data
